const { When, Then } = require('@cucumber/cucumber')
const { expect } = require('@playwright/test');
const { POManager } = require('../../pageObjects/POManager');


When('Login into the application',{timeout : 100*1000}, async function () {
   
  });

